/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'placehold.co', "img.youtube.com", 'www.attensys.ai', 'tedxtalkcu.com']
    },
};

export default nextConfig;
